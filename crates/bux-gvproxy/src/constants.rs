//! Virtual network topology — re-exported from [`bux_proto::net`].

pub use bux_proto::net::{
    DEFAULT_MTU, DNS_SEARCH_DOMAINS, DNS_SERVER_IP, GATEWAY_IP, GATEWAY_MAC, GATEWAY_MAC_STRING,
    GUEST_CIDR, GUEST_INTERFACE, GUEST_IP, GUEST_MAC, GUEST_MAC_STRING, SUBNET, mac_to_string,
};
