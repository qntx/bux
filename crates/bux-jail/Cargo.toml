[package]
name = "bux-jail"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Process isolation for the bux-shim (bwrap / seatbelt / pre_exec)"
keywords = ["sandbox", "jail", "bubblewrap", "seatbelt", "vm"]
categories = ["os", "virtualization"]
readme = "README.md"

[dependencies]
thiserror.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
bux-bwrap.workspace = true
bux-landlock = { path = "../bux-landlock", version = "0.1" }

[lints]
workspace = true
